/*Minimum Size Subarray*/
#include <iostream>
#include <vector>

using namespace std;
int main()
{
    int sum, length, temp;
    vector<int> arr;
    cin >> length >> sum;
    while (cin >> temp)
    {
        arr.push_back(temp);
    }

        return 0;
}